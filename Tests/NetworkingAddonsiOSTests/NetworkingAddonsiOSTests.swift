import XCTest
@testable import NetworkingAddonsiOS

final class NetworkingAddonsiOSTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(NetworkingAddonsiOS().text, "Hello, World!")
    }
}
